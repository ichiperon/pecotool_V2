# PecoTool V2 — 要件定義書

## 1. プロジェクト概要

### 1.1 目的
PDF に付与された OCR テキスト（透明テキストレイヤー）を、メモ帳で文字を直すような感覚で自由に編集できるデスクトップツールを開発する。

### 1.2 背景・課題
- 既存の OCR 編集ツールは「再 OCR」「文字読み取り」が中心であり、手動で誤字脱字を直す機能に特化したツールが存在しない。
- スキャン PDF の OCR 品質が低い場合、1 文字ずつバラバラのバウンディングボックスになる・読み順が狂うなどの問題があるが、これを手軽に修正する手段がない。
- 数千ページ規模の PDF を扱う業務で、既存ツールではメモリ逼迫やフリーズが発生する。

### 1.3 対象ユーザー
- 一般家庭の PC を使用するユーザー（高スペックマシンを前提としない）。
- PDF の OCR 品質を手動で改善したいユーザー（文書管理者、アーカイブ担当者等）。
- 技術的な専門知識を持たない一般ユーザーでも操作できることを前提とする。

### 1.4 前提・制約

| 項目 | 内容 |
|------|------|
| 編集の定義 | 再 OCR や文字認識機能ではなく、既存 OCR テキストの手動修正（誤字脱字の手打ち修正） |
| パフォーマンス要件 | 数千ページ規模の PDF でもフリーズ・遅延なく動作すること |
| メモリ制約 | 一般家庭 PC（RAM 8–16 GB 想定）で問題なく動作すること |
| 対象 OS | Windows（最低限）。可能であればクロスプラットフォーム |
| 対象 PDF | OCR テキストレイヤー付きの PDF（画像 + 透明テキスト構成） |
| 対象外 | テキストベース PDF（Word 等から生成された PDF）の編集は対象外 |
| 同時オープン | 1 ファイルずつの編集を想定（マルチドキュメントは対象外） |

### 1.5 スコープ外
- OCR エンジンの搭載（文字認識処理そのものは行わない）
- PDF のページ追加・削除・回転等のページ操作
- PDF の画像レイヤー編集（墨消し・注釈等）
- PDF のパスワード保護・暗号化処理
- クラウド連携・マルチユーザー機能

---

## 2. 機能要件

### 2.1 必須機能（Must）

---

#### F-01: PDF 読み込み・表示

**概要**
数百〜数千ページの OCR 付き PDF を読み込み、ページ画像を表示する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 対応形式 | PDF 1.3〜2.0。OCR テキストレイヤー（透明テキスト）を含むもの。※ PDF 1.3 は業務用スキャナーで生成される場合があるため対象に含める |
| ファイルオープン方法 | ① ツールバーの「開く」ボタン ② PDF 画像表示エリアへのドラッグ＆ドロップ ③ ファイルの関連付け（ダブルクリック） |
| 最大ページ数 | 制限なし（メモリ管理により実質的に数千ページ対応） |
| 最大ファイルサイズ | 制限なし（遅延読み込みによりファイルサイズに依存しない） |

**読み込み戦略**

```
1. ファイルオープン時
   ├─ PDF メタデータ（総ページ数・ページサイズ等）を即座に取得
   ├─ 1 ページ目の画像 + OCR データを読み込み → 表示
   └─ サムネイルは可視範囲のみ生成開始（バックグラウンド）

2. ページ遷移時
   ├─ 表示対象ページの画像 + OCR データを取得
   ├─ 前後 ±2 ページをプリフェッチ（バックグラウンド）
   └─ 表示範囲外のページデータを LRU ポリシーで解放

3. サムネイル
   ├─ 仮想スクロール：可視範囲 + 上下バッファ分のみ DOM に描画
   └─ サムネイル画像は低解像度（長辺 150px 程度）で生成しキャッシュ
```

**受入条件**
- [ ] 1,000 ページの PDF を開いて 3 秒以内に 1 ページ目が表示される
- [ ] 1,000 ページ表示時のメモリ使用量が 500 MB 以下
- [ ] ページ遷移が 200 ms 以内に完了する
- [ ] ドラッグ＆ドロップで PDF を開ける
- [ ] OCR テキストレイヤーのない PDF を開いた場合、画像のみ表示しエラーにならない

**エラーハンドリング**
- 破損 PDF：「ファイルを読み込めません。PDF が破損している可能性があります。」と表示
- パスワード付き PDF：「パスワード付き PDF には対応していません。」と表示
- 非 PDF ファイル：「PDF ファイルを選択してください。」と表示

---

#### F-02: OCR テキスト編集

**概要**
PDF の透明テキストレイヤーから OCR テキストを抽出し、テキストエリアに表示。手動で修正できる。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 表示単位 | バウンディングボックス単位で区切って表示 |
| 編集操作 | テキストのインライン編集（クリックでフォーカス → 手打ち修正） |
| 対応文字方向 | 縦書き（vertical-rl）・横書き（horizontal-tb） |
| 文字方向の判定 | バウンディングボックスの縦横比 + テキストの配置方向から自動判定 |
| 文字方向の表示 | テキスト編集エリアでは横書き表示に統一（編集のしやすさ優先）。ただし縦書き/横書きのラベルを表示 |
| 編集中の視覚フィードバック | 編集中のバウンディングボックスを PDF 表示エリアでハイライト |
| 未保存変更の表示 | 変更があるバウンディングボックスに「●（未保存マーク）」を表示 |

**ユーザー操作フロー**

```
1. ページを表示すると、右エリアに OCR テキストがバウンディングボックス単位で一覧表示される
2. 修正したいテキストブロックをクリック → 編集モードに入る
3. テキストを手打ちで修正する（メモ帳と同じ感覚）
4. Enter または別のブロックをクリックで確定
5. 確定された変更は Undo/Redo スタックに積まれる
6. 保存操作で PDF に反映
```

**受入条件**
- [ ] 各ページの OCR テキストがバウンディングボックス単位で表示される
- [ ] テキストをクリックして手打ちで修正できる
- [ ] 縦書き OCR のテキストも正しく表示・編集できる
- [ ] 編集中のテキストに対応する PDF 上のバウンディングボックスがハイライトされる
- [ ] 空のバウンディングボックス（テキストなし）も表示される（ただしカードのテキスト部分に「（空）」プレースホルダーを表示し、大量の空 BB がある場合は折りたたみ表示で一覧性を損なわない）

---

#### F-03: バウンディングボックスのグループ化

**概要**
1 文字ずつ分かれたバウンディングボックスを選択し、1 つのバウンディングボックスに統合する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 選択方法 | ① テキスト編集エリアで Shift+クリック / Ctrl+クリックで複数選択 ② PDF 表示エリアでドラッグ矩形選択 |
| グループ化実行 | 選択後に右クリックメニューまたはツールバーの「グループ化」ボタン |
| 座標の統合 | 選択されたバウンディングボックスの外接矩形を新しいバウンディングボックスとする |
| テキストの統合順序 | **選択操作に依存して決定する（下記参照）** |
| 文字方向 | 統合対象の多数派の方向を継承。異なる方向が混在する場合は警告を表示 |
| 統合上限 | 制限なし |
| グループ化後の BB 座標調整 | グループ化後も PDF 画像上でドラッグして位置・サイズを手動調整可能（F-07 の位置調整と同じ操作） |

**テキスト結合順序のルール**

| 選択方法 | 結合順序 |
|---------|---------|
| Ctrl+クリック | クリックした順序で結合 |
| Shift+クリック（範囲選択） | テキスト編集エリアでの表示順（上→下）で結合 |
| PDF 表示エリアでドラッグ矩形選択（横書き BB） | 左上→右下の座標順（Y 座標優先 → X 座標）で結合 |
| PDF 表示エリアでドラッグ矩形選択（縦書き BB） | 右上→左下の座標順（X 座標降順 → Y 座標）で結合 |

> ドラッグ矩形選択の場合、選択開始点の方向（左から右 or 右から左）は結合順に影響しない。BB の座標位置で一意に決定する。
> Ctrl+クリックの場合はユーザーの意図を尊重し、クリック順をそのまま採用する。

**グループ化の例**

```
Before:
  [BB-1] "あ"  (x:10, y:10, w:20, h:20)
  [BB-2] "い"  (x:30, y:10, w:20, h:20)
  [BB-3] "う"  (x:50, y:10, w:20, h:20)

After (BB-1, BB-2, BB-3 を選択してグループ化):
  [BB-1'] "あいう"  (x:10, y:10, w:60, h:20)  ← 外接矩形
```

**受入条件**
- [ ] 複数のバウンディングボックスを選択してグループ化できる
- [ ] グループ化後のテキストは上記ルールに従った順序で結合される
- [ ] グループ化後の座標は外接矩形になる
- [ ] グループ化後に BB の位置・サイズを手動調整できる
- [ ] グループ化は Undo で元に戻せる（= 分割操作として機能する）
- [ ] 1 つのバウンディングボックスのみ選択時は「グループ化」が無効（グレーアウト）

---

#### F-04: OCR テキスト削除

**概要**
指定したページまたは範囲の OCR テキストを一括削除する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 削除単位 | ① 個別バウンディングボックス ② 現在のページ全体 ③ 指定ページ範囲（例: 5–20 ページ） ④ 全ページ |
| 個別削除 | テキスト編集エリアで選択 → Delete キーまたは右クリック「削除」 |
| ページ単位削除 | ツールバーまたは右クリックメニューから「このページの OCR を削除」 |
| 範囲削除 | ダイアログで開始ページ・終了ページを指定 |
| 確認 | ページ単位以上の削除は確認ダイアログを表示（「X ページの OCR テキストを削除します。よろしいですか？」） |
| 結果 | 削除後は OCR なしの画像のみのページとなる |

**受入条件**
- [ ] 個別のバウンディングボックスを削除できる
- [ ] ページ全体の OCR を一括削除できる
- [ ] ページ範囲を指定して一括削除できる
- [ ] 削除操作は Undo で元に戻せる
- [ ] ページ単位以上の削除時に確認ダイアログが表示される

---

#### F-05: テキスト並び順の整合性保証

**概要**
ツール内で表示されるテキストの並び順と、保存後の PDF からコピペした際のテキスト順序を完全に一致させる。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 順序の定義 | テキスト編集エリアでの上から下への並び順 = PDF テキストレイヤーの読み順 |
| 順序の決定方法 | **Content Stream 内のテキストオブジェクト出現順を正とする**。Structure Tree が存在する場合でも Content Stream 順を優先する（理由: コピペ順序を直接制御するのは Content Stream であり、Structure Tree は Acrobat 等のアクセシビリティ向け情報のため） |
| 検証方法 | 保存後の PDF を Acrobat 等で開き Ctrl+A → Ctrl+C した結果がツール内の順序と一致 |
| プレビュー機能 | テキスト編集エリアの表示がそのままコピペ結果のプレビューとなる |

**コピペ順序の制御**

```
PDF テキストレイヤーの構造:

  /Content Stream
    ├─ TextBlock[0] "タイトル"       ← コピペ時 1 番目
    ├─ TextBlock[1] "本文1行目"      ← コピペ時 2 番目
    ├─ TextBlock[2] "本文2行目"      ← コピペ時 3 番目
    └─ TextBlock[3] "ページ番号"     ← コピペ時 4 番目

  ↕ この順序がテキスト編集エリアの並び順と一致する
```

**受入条件**
- [ ] テキスト編集エリアの並び順で保存後の PDF がコピペされる
- [ ] 読み込み直後の並び順は元の PDF のコピペ順と一致する
- [ ] 並べ替え後に保存 → 再度開いた際に並べ替え後の順序が保持されている

---

#### F-06: バウンディングボックスの並び順変更

**概要**
バウンディングボックスの読み順をドラッグ＆ドロップで変更する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 操作方法 | テキスト編集エリアでバウンディングボックスをドラッグ＆ドロップ |
| 複数選択移動 | Shift/Ctrl 選択した複数ブロックを一括で移動可能 |
| 視覚フィードバック | ドラッグ中はドロップ先を示すインジケーター（挿入線）を表示 |
| 即時反映 | 並べ替えは即座にプレビューに反映（保存は別操作） |
| PDF 表示連動 | （Nice to Have）並べ替え後、PDF 表示エリアで読み順の番号をオーバーレイ表示 |

**ユースケース**

```
Before: "画像キャプション" → "本文" → "タイトル" → "脚注"
（OCR が読み取り順を誤っている）

操作: "タイトル" を先頭にドラッグ、"脚注" を末尾にドラッグ

After: "タイトル" → "本文" → "画像キャプション" → "脚注"
（正しい読み順に修正）
```

**受入条件**
- [ ] ドラッグ＆ドロップでバウンディングボックスの順序を変更できる
- [ ] 複数ブロックを一括で移動できる
- [ ] 並べ替えは Undo で元に戻せる
- [ ] 並べ替え後に保存した PDF のコピペ順が新しい順序を反映している

---

#### F-07: バウンディングボックスの追加・手打ち入力

**概要**
PDF 画像上の任意の位置に新しいバウンディングボックスを追加し、テキストを入力する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 追加方法 | ① ツールバーの「追加」ボタン → PDF 画像上でドラッグして矩形を作成 ② 右クリックメニュー「ここにテキストを追加」 |
| テキスト入力 | 矩形作成後、テキスト編集エリアに新規ブロックが追加されフォーカスされる |
| 位置調整 | 追加後も PDF 画像上でドラッグして位置・サイズを調整可能。**既存 BB（読み込み時から存在するもの）も同様に位置・サイズ調整が可能** |
| 読み順 | 新規ブロックは BB の座標位置から読み順を自動推定して挿入する（横書き: 左上→右下、縦書き: 右上→左下）。推定位置が不適切な場合はドラッグで任意の位置に移動可能（F-06）。自動推定が困難な場合（既存 BB と座標が大きく離れている等）は末尾に追加 |
| 文字方向 | 追加時に縦書き/横書きを選択（デフォルト: 横書き） |
| 既存 BB とのグループ化 | 追加した BB を既存の BB と選択してグループ化可能（F-03） |

**ユーザー操作フロー**

```
1. ツールバーの「追加」ボタンをクリック → カーソルが十字に変わる
2. PDF 画像上でドラッグして矩形を描画
3. テキスト編集エリアに空のブロックが追加される
4. テキストを手打ちで入力
5. 必要に応じて読み順をドラッグで調整
6. 必要に応じて既存 BB とグループ化
```

**受入条件**
- [ ] PDF 画像上でドラッグして新規バウンディングボックスを作成できる
- [ ] 作成後にテキストを入力できる
- [ ] 追加した BB の位置・サイズを後から調整できる
- [ ] 追加した BB を既存の BB とグループ化できる
- [ ] 追加操作は Undo で元に戻せる

---

#### F-08: PDF 保存

**概要**
編集内容を反映した PDF を保存する。保存後の PDF は編集後のテキストレイヤーで検索・コピペが可能。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 保存方法 | ① 上書き保存（Ctrl+S） ② 名前を付けて保存（Ctrl+Shift+S） |
| 保存対象 | テキストレイヤー（バウンディングボックス座標 + テキスト + 読み順）のみ |
| 画像レイヤー | 一切変更しない（無劣化で保持） |
| 保存処理 | 一時ファイルに書き出し → 検証 → リネームで置き換え（書き込み中断による破損を防止） |
| 進捗表示 | 大容量 PDF の保存時はプログレスバーを表示 |
| 初回保存 | 新規に「名前を付けて保存」ダイアログを表示 |
| 保存と Undo の関係 | 保存後も Undo 履歴は保持する。保存後に Undo で保存前の状態に戻した場合、isDirty フラグが再び true になる（未保存マーク再表示）。その状態で Ctrl+S すると Undo 後の状態で上書き保存される。**保存操作自体は Undo 対象外**であり、「保存を取り消す」機能は提供しない。また、初期読み込みや表示用のページデータマージ（`isDirty: false` 指定時）では全体の `isDirty` フラグは更新されない。 |

**保存時の処理フロー**

```
1. 編集データの整合性チェック
   ├─ 空テキストの BB が存在する場合 → 警告（「空のテキストブロックがあります。保存しますか？」）
   └─ 座標が重複する BB → そのまま保存（警告なし）

2. 一時ファイルへの書き出し
   ├─ 元 PDF を複製
   ├─ 各ページのテキストレイヤーを編集後データで再構築
   │   ├─ テキスト内容の更新
   │   ├─ バウンディングボックス座標の更新
   │   └─ 読み順（Content Stream 内の順序）の更新
   └─ 画像レイヤーはストリームをそのままコピー（無変換）

3. 検証
   └─ 一時ファイルが正常な PDF として読み込めるか検証

4. リネーム
   └─ 一時ファイルを正式ファイル名にリネーム（アトミック操作）
```

**受入条件**
- [ ] Ctrl+S で上書き保存できる
- [ ] Ctrl+Shift+S で名前を付けて保存できる
- [ ] 保存後の PDF を Acrobat 等で開いて、編集後のテキストでコピペ・検索ができる
- [ ] 保存前後で画像品質が劣化しない
- [ ] 保存中にアプリが異常終了しても元の PDF が破損しない
- [ ] 未保存の変更がある状態でアプリを閉じようとすると確認ダイアログが表示される

---

#### F-11: バウンディングボックスの分割機能

**概要**
既存のバウンディングボックスをクリックした位置で分割し、2つのバウンディングボックスに分ける。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 操作方法 | ツールバーの「分割」ボタンをクリック（分割モード） → PDF上の対象テキスト枠をクリック |
| 分割ロジック | クリックされた座標位置（横書きはX軸、縦書きはY軸）に基づき、文字の幅の比率から分割位置となる文字インデックスを計算し、テキストとバウンディングボックスを2つに分割する |
| 連動 | 分割後の新しいブロックはプレビューや元データに即時反映される |

**受入条件**
- [ ] 分割モード中にテキストをクリックすると適切な位置で2つに分割される
- [ ] 分割操作は Undo で元に戻せる

---

#### F-12: OCR テキスト重複削除（レイヤー整理）

**概要**
同一位置に重なっている重複したOCRテキストレイヤー（バウンディングボックス）を自動で判定・削除する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 操作方法 | ツールバーの「重複削除」ボタンをクリック |
| 判定基準 | テキスト内容が完全に一致しており、かつ BoundingBox 座標（x, y, width, height）の差異が極めて小さいものを「重複」とみなす |
| 対象 | 選択中のBBがある場合は、選択されているBBと他のBBの間で判定。未選択時はページ全体を対象 |

**受入条件**
- [ ] 重複するBBがある場合、余分なBBが削除されて1つにまとめられる
- [ ] 重複削除操作は Undo で元に戻せる

---

#### F-14: OCR 実行（テキストレイヤー自動生成）

**概要**
OCR テキストレイヤーを持たないページに対して、組み込みの OCR エンジンでテキストを自動生成する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 操作方法 | ツールバーの「OCR実行」ボタンをクリックし、「現在のページ」または「全ページ」を選択 |
| 実行対象 | 「現在のページ」または「全ページ」の 2 択 |
| 進捗表示 | 実行中はボタンをスピナーに変更し「OCR X/Y」で進捗を表示 |
| キャンセル | 実行中に「キャンセル」ボタンで中断可能 |
| 結果 | 認識結果が TextBlock として現在のページデータに追加される |

**受入条件**
- [ ] 「現在のページ」を選択すると現在のページに OCR を実行できる
- [ ] 「全ページ」を選択すると全ページに順次 OCR を実行できる
- [ ] 実行中は進捗（X/Y ページ）が表示される
- [ ] キャンセルボタンで中断できる
- [ ] OCR 結果は Undo で元に戻せる

---

#### F-13: リアルタイムテキストプレビューウィンドウ

**概要**
PDF全体のOCRテキストの読み順や改行を、別ウィンドウでリアルタイムにプレビュー表示する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 操作方法 | ツールバーの「別ウインドウで確認」ボタンをクリックで開く |
| 表示内容 | 現在のページのテキストボックスの並び順に従って、段落や改行を加味した文字列を結合表示 |
| ライフサイクル | メインウィンドウが閉じられた際に、プレビューウィンドウも自動的に閉じられる（プロセス連動） |

**受入条件**
- [ ] ツールバーの「テキスト確認」ボタンをクリックするとプレビューウィンドウが開く
- [ ] メイン画面での編集（並び替え・分割・テキスト修正など）がリアルタイムにプレビューに反映される
- [ ] アプリ本体を終了するとプレビューウィンドウも閉じる

---

### 2.2 あると良い機能（Nice to Have）

---

#### F-10: ページ内テキスト検索

**概要**
OCR テキスト編集エリア上部の検索ボックスで、現在ページ内の BB テキストを検索・フィルタリングする。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 検索範囲 | 現在表示中のページ内の BB テキスト |
| 検索方法 | インクリメンタルサーチ（入力中にリアルタイムでフィルタリング） |
| 一致表示 | 一致する BB カードをハイライト。不一致のカードはグレーアウトまたは非表示 |
| ショートカット | Ctrl+F で検索ボックスにフォーカス。Esc で検索解除 |

**受入条件**
- [ ] 検索ボックスに文字を入力すると一致する BB がフィルタリングされる
- [ ] 検索解除で元の全 BB 表示に戻る
- [ ] 100 個以上の BB があるページでも検索が遅延しない

---

#### F-09: OCR 可視化オーバーレイ

**概要**
PDF 画像上に OCR バウンディングボックスを半透明カラーでオーバーレイ表示する。

**詳細仕様**

| 項目 | 仕様 |
|------|------|
| 表示色 | 薄赤色（rgba 255, 0, 0, 0.15）をデフォルト |
| 選択中の BB | 強調色（rgba 0, 120, 255, 0.3）で表示 |
| トグル | ツールバーのボタンまたは Ctrl+H で ON/OFF 切替 |
| パフォーマンス | Canvas オーバーレイで描画（DOM 要素は使用しない） |
| BB の境界線 | 1px の実線で各 BB の境界を表示 |

**受入条件**
- [ ] OCR バウンディングボックスが半透明のオーバーレイで表示される
- [ ] 選択中の BB が異なる色でハイライトされる
- [ ] ON/OFF をトグルで切り替えられる
- [ ] 100 個以上の BB があるページでも描画が遅延しない

---

### 2.3 全機能共通仕様

#### Undo/Redo

| 項目 | 仕様 |
|------|------|
| スコープ | ページ単位ではなくドキュメント全体で 1 つの履歴スタック |
| 対象操作 | テキスト編集 / グループ化 / 削除 / 追加 / 並べ替え / BB 位置・サイズ調整 |
| 履歴上限 | 100 操作（超過分は古い順に破棄。**破棄時にユーザーへの通知は行わない** — 操作の一貫性を保つため、100 は十分な量と判断） |
| ショートカット | Undo: Ctrl+Z / Redo: Ctrl+Y（または Ctrl+Shift+Z） |
| 保存後の挙動 | 保存後も Undo 履歴は保持（保存操作自体は Undo 対象外）。保存後に Undo すると isDirty が true に戻る |

#### キーボードショートカット一覧

| カテゴリ | ショートカット | 操作 |
|---------|--------------|------|
| ファイル | Ctrl+O | ファイルを開く |
| ファイル | Ctrl+S | 上書き保存 |
| ファイル | Ctrl+Shift+S | 名前を付けて保存 |
| 編集 | Ctrl+Z | 元に戻す |
| 編集 | Ctrl+Y / Ctrl+Shift+Z | やり直し |
| 編集 | Delete | 選択中の BB を削除 |
| 編集 | Ctrl+C | 選択中の BB をコピー |
| 編集 | Ctrl+V | BB を貼り付け |
| 編集 | Ctrl+Shift+Space | 選択中の BB のスペースを削除 |
| 操作 | Ctrl+F10 | BB 追加モードの切替 |
| 操作 | Ctrl+F11 | BB 分割モードの切替 |
| 操作 | Ctrl+F12 | グループ化 |
| 選択 | Ctrl+Click | BB の追加選択 |
| 選択 | Shift+Click | BB の範囲選択 |
| 検索 | Ctrl+F | 検索ボックスにフォーカス |
| 表示 | Ctrl+マウスホイール上 | ズームイン |
| 表示 | Ctrl+マウスホイール下 | ズームアウト |
| 表示 | Ctrl+0 | ズームリセット（フィット表示） |
| ヘルプ | 右クリック（空白エリア） | ショートカットヘルプを表示 |

#### 右クリックコンテキストメニュー

アプリ上の任意の場所を右クリックすると、キーボードショートカット一覧のヘルプパネルを表示する。

**テキスト編集エリア（OcrCard）上**
- 右クリックで未選択の BB を選択状態にする（コンテキストメニューは表示しない）

---

## 3. 非機能要件

### 3.1 パフォーマンス

| 指標 | 目標値 | 測定条件 |
|------|--------|---------|
| 初期表示 | 3 秒以内 | 1,000 ページ PDF、1 ページ目の画像 + OCR テキストが操作可能な状態 |
| ページ遷移 | 200 ms 以内 | キャッシュ済みページ。未キャッシュの場合 500 ms 以内 |
| メモリ使用量 | 500 MB 以下 | 1,000 ページ PDF 表示中（ピーク値） |
| メモリ使用量 | 1 GB 以下 | 3,000 ページ PDF 表示中（ピーク値） |
| サムネイルスクロール | 60 fps | 1,000 ページ分のサムネイルを高速スクロール中 |
| テキスト編集レスポンス | 入力遅延なし | 100 個の BB を持つページでテキストを編集中 |
| 保存処理 | 30 秒以内 | 1,000 ページ PDF の保存完了 |
| アプリ起動 | 2 秒以内 | ファイル未オープン状態での起動完了 |

### 3.2 パフォーマンス実現方針

#### メモリ管理戦略

```
                    メモリ上に保持
              ┌──────────────────────┐
              │                      │
  ... [P3] [P4] | [P5] [★P6] [P7] | [P8] [P9] ...
              │   バッファ  表示中  バッファ │
              └──────────────────────┘
                 他は LRU キャッシュで管理
                 （キャッシュ上限: 50 ページ分）
```

- **アクティブウィンドウ**: 表示中ページ ± 2 ページ（計 5 ページ）を高解像度でメモリ保持
- **LRU キャッシュ**: 直近アクセスした 50 ページ分のレンダリング結果をキャッシュ
- **キャッシュ超過時**: 最も古いアクセスのページから画像データを解放
- **OCR データ**: 表示中ページの OCR データのみメモリ保持。編集済みページの差分は軽量な JSON で保持
- **サムネイル**: 低解像度（長辺 150px）で生成。全ページ分をキャッシュしても数十 MB 程度

#### スレッド戦略

| スレッド | 担当処理 |
|---------|---------|
| メインスレッド（UI） | ユーザー操作の受付・画面描画・テキスト編集 |
| Worker 1: PDF レンダラー | ページ画像のレンダリング（プリフェッチ含む） |
| Worker 2: サムネイル生成 | サムネイル画像のバックグラウンド生成 |
| Worker 3: PDF 保存 | 保存処理（メインスレッドをブロックしない） |

### 3.3 信頼性

| 項目 | 仕様 |
|------|------|
| 保存の原子性 | 一時ファイル → リネーム方式で保存中断による破損を防止 |

#### 自動バックアップ

| 項目 | 仕様 |
|------|------|
| 保存間隔 | 最後の編集操作から 60 秒後に自動保存（操作が続いている間はタイマーリセット） |
| 保存先 | 元 PDF と同じディレクトリに `.pecotool_backup/` フォルダを作成 |
| ファイル命名 | `{元ファイル名}.peco-backup.json`（編集差分のみ保存。PDF 本体はコピーしない） |
| バックアップ内容 | 編集済み TextBlock の差分（変更されたページの textBlocks + Undo スタック） |
| 削除タイミング | ① ユーザーが明示的に保存（Ctrl+S）した時点で削除 ② アプリ正常終了時に削除 |
| 「名前を付けて保存」時 | 新しいパスに保存後、旧パスのバックアップを削除。以降のバックアップは新パスのディレクトリに作成 |
| クラッシュリカバリ | 起動時に `.pecotool_backup/` 内のバックアップを検出した場合、ダイアログで「復旧する / 破棄する」を選択 |

#### ファイルロック

| 項目 | 仕様 |
|------|------|
| ロック方式 | 元 PDF と同じディレクトリに `{元ファイル名}.peco-lock` ファイルを作成（ロックファイル方式） |
| ロック内容 | プロセス ID + タイムスタンプを記録 |
| ロック取得失敗時 | 「このファイルは別のプロセスで編集中です。読み取り専用で開きますか？」ダイアログを表示。読み取り専用モードでは編集・保存操作が無効化される |
| ロック解放 | アプリ終了時・ファイルクローズ時にロックファイルを削除 |
| 古いロックの処理 | ロックファイルのプロセス ID が存在しないプロセスの場合（= 前回のクラッシュ残骸）、ロックを無視して取得 |

### 3.4 ユーザビリティ

| 項目 | 仕様 |
|------|------|
| 初見操作 | マニュアルなしで基本操作（PDF 読み込み → テキスト編集 → 保存）が完了できる |
| フィードバック | 全ての操作に対して即座に視覚的フィードバックを返す |
| エラー回復 | 全ての編集操作は Undo で元に戻せる |
| 操作方法 | ドラッグ＆ドロップ・右クリックメニュー・キーボードショートカットの 3 系統 |
| 状態表示 | ステータスバーに現在ページ番号・総ページ数・ズーム率・未保存マークを表示 |
| 最近のファイル | **直近 10 件** のファイル履歴を「ファイル」メニューに表示。localStorage に保存 |

---

## 4. 画面構成

### 4.1 全体レイアウト

```
┌──────────────────────────────────────────────────────────────────┐
│  ツールバー                                                       │  ← 上部固定
├───────┬──────────────────────────────┬───────────────────────────┤
│       │                              │                           │
│ サム  │                              │  OCR テキスト             │
│ ネイル│    PDF 画像表示エリア          │  表示/編集エリア           │
│ 表示  │    （メインビュー）            │                           │
│ エリア│                              │                           │
│       │                              │                           │
│ 比率1 │         比率 5               │        比率 4             │
│       │                              │                           │
│       │                              │                           │
│       │                              │                           │
├───────┴──────────────────────────────┴───────────────────────────┤
│  ステータスバー                                                    │  ← 下部固定
└──────────────────────────────────────────────────────────────────┘
```

> **比率（横方向）**：サムネイル:PDF 表示:テキスト編集 = 1:5:4 で横並び 3 カラム構成。
> カラム間の境界はドラッグで幅を調整可能（スプリッター）。

### 4.2 各エリア詳細

#### A. ツールバー（上部固定）

```
┌────────────────────────────────────────────────────────────────┐
│ [↩Undo] [↪Redo]  |  [🔍+] [🔍-] [Fit]                         │
│ |  [＋BB追加] [✂分割] [📦グループ化] [🧹重複削除] [🔤スペース削除] [🗑削除] │
│ |  [👁OCR表示] [⚙設定] [📋テキスト確認]  |  [🔍OCR実行]         │
└────────────────────────────────────────────────────────────────┘
```

- アイコン + テキストラベルのボタン配置
- セパレーターで機能グループを区切り
- 操作不可時はグレーアウト（例: 未選択時にグループ化ボタンが無効）

#### B. サムネイル表示エリア（左カラム・比率 1）

| 項目 | 仕様 |
|------|------|
| 表示 | ページサムネイルを縦一列に並べる（Acrobat 風） |
| サムネイルサイズ | カラム幅に合わせて自動調整。ページ番号をサムネイル下部に表示 |
| 選択状態 | 現在表示中のページを青枠でハイライト |
| 未保存マーク | 編集済みページのサムネイルに小さなマーク（●）を表示 |
| スクロール | 仮想スクロール。可視範囲 + 上下 5 件のバッファのみ描画 |
| ページジャンプ | クリックで即座にジャンプ |
| キーボード | ↑←: 前ページ / ↓→: 次ページ / Home: 先頭 / End: 末尾 |
| 幅の変更 | 左右のスプリッターをドラッグして幅を調整可能 |

#### C. PDF 画像表示エリア（中央カラム・比率 5）

| 項目 | 仕様 |
|------|------|
| 表示 | 選択中ページの PDF 画像を Canvas で描画 |
| ズーム | Ctrl+マウスホイール / ツールバーボタン。25%–400% の範囲 |
| フィット表示 | エリア幅に合わせて自動フィット（デフォルト） |
| パン操作 | ズーム時に Space + ドラッグでスクロール |
| ドラッグ＆ドロップ | PDF ファイルをドロップしてオープン |
| BB 追加モード | ツールバーの「追加」ボタンクリック後、画像上でドラッグして矩形を描画 |
| OCR オーバーレイ | （Nice to Have）半透明矩形でバウンディングボックスを表示 |
| 選択連動 | テキスト編集エリアで選択中の BB を PDF 画像上でハイライト |
| ファイル未オープン時 | 「PDF ファイルをドラッグ＆ドロップまたは Ctrl+O で開く」のプレースホルダーを表示 |

#### D. OCR テキスト表示/編集エリア（右カラム・比率 4）

| 項目 | 仕様 |
|------|------|
| 表示 | バウンディングボックス単位でカード形式にテキストを一覧表示 |
| カード構成 | ヘッダー: BB 番号 + 文字方向ラベル（横/縦） + 未保存マーク。本体: テキスト |
| テキスト編集 | カードのテキスト部分をクリック → インライン編集モード |
| 並べ替え | カードをドラッグ＆ドロップで順序変更 |
| 複数選択 | Ctrl+クリック / Shift+クリックで複数カードを選択 |
| グループ化操作 | 複数選択 → 右クリック「グループ化」 |
| 削除操作 | 選択 → Delete キーまたは右クリック「削除」 |
| 検索 | エリア上部に検索ボックス。ページ内の BB テキストをフィルタリング |
| OCR なしページ | 「このページには OCR テキストがありません」のメッセージを表示 |
| スクロール | BB 数が多い場合は縦スクロール |

**カード表示イメージ**

```
┌─────────────────────────────────┐
│ #1  横書き  ●                    │  ← ヘッダー（BB番号・方向・未保存マーク）
│ ─────────────────────────────── │
│ これは編集可能なOCRテキストです     │  ← 本体（クリックで編集可能）
└─────────────────────────────────┘
┌─────────────────────────────────┐
│ #2  縦書き                       │
│ ─────────────────────────────── │
│ 右から左に読む文章                 │
└─────────────────────────────────┘
     :
```

#### E. ステータスバー（下部固定）

```
┌────────────────────────────────────────────────────────────────┐
│ ページ: 42 / 1,024  |  ズーム: 100%  |  BB数: 15  |  ● 未保存  │
└────────────────────────────────────────────────────────────────┘
```

- 現在ページ / 総ページ数
- ズーム率
- 現在ページの BB 数
- 未保存変更の有無

### 4.3 画面状態遷移

```
[起動]
  │
  ▼
[初期画面]──── ファイル未オープン。PDF 表示エリアにプレースホルダー
  │
  │ PDF オープン（ドラッグ＆ドロップ / Ctrl+O / 関連付け起動）
  ▼
[編集画面]──── 3 カラム表示。サムネイル + PDF + テキスト編集
  │
  ├─ テキスト編集 → [編集中]（未保存マーク表示）
  ├─ 保存 → [保存完了]（未保存マーク消去）
  ├─ 別ファイルを開く → 未保存なら確認ダイアログ → [編集画面]
  └─ ファイルを閉じる → 未保存なら確認ダイアログ → [初期画面]
  │
  │ アプリ終了
  ▼
[終了]──── 未保存なら確認ダイアログ
```

---

## 5. データモデル

### 5.1 内部データ構造

```
PecoDocument
├── filePath: string                    // 元 PDF のファイルパス
├── fileName: string                    // ファイル名（表示用）
├── totalPages: number                  // 総ページ数
├── metadata: PDFMetadata               // PDF メタデータ
├── pages: Map<pageIndex, PageData>     // ページデータ（遅延読み込み）
└── mtime?: number                      // ファイルの最終更新時刻（ms）

PageData
├── pageIndex: number                   // ページ番号（0-based）
├── width: number                       // ページ幅（pt）
├── height: number                      // ページ高さ（pt）
├── textBlocks: TextBlock[]             // バウンディングボックス一覧（読み順）
├── isDirty: boolean                    // 未保存の編集があるか
└── thumbnail: string | null            // サムネイル画像（Base64 または Blob URL）

TextBlock
├── id: string                          // 一意識別子（UUID）
├── text: string                        // テキスト内容
├── originalText: string                // 元のテキスト（差分検出用）
├── bbox: BoundingBox                   // バウンディングボックス座標
├── writingMode: "horizontal" | "vertical"  // 文字方向
├── order: number                       // 読み順（0-based）
├── isNew: boolean                      // 新規追加された BB か
├── isDirty: boolean                    // このブロックに未保存の変更があるか
└── children?: string[]                 // グループ化元の BB ID 一覧（グループ化後のみ）

BoundingBox
├── x: number                           // 左上 X 座標（pt）
├── y: number                           // 左上 Y 座標（pt）
├── width: number                       // 幅（pt）
└── height: number                      // 高さ（pt）

UndoStack（ストアで管理）
├── undoStack: Action[]                 // Undo 可能な操作（max 100）
├── redoStack: Action[]                 // Redo 可能な操作
└── Action: { type: 'update_page', pageIndex, before: PageData, after: PageData }
```

### 5.2 データライフサイクル

```
         読み込み                 編集                    保存
PDF ──────────→ PecoDocument ──────────→ 編集済み状態 ──────────→ PDF
 (ファイル)       (メモリ)        (メモリ上で変更)        (ファイル)

  ・PDF パーサーが              ・TextBlock の text,     ・PDF ライターが
    テキストレイヤーを解析        bbox, order を変更       テキストレイヤーを
  ・TextBlock[] に変換          ・Undo スタックに記録      再構築して書き出し
```

---

## 6. データフロー

### 6.1 PDF 読み込みフロー

```
ユーザー操作: PDF をオープン
       │
       ▼
┌──────────────────┐
│ ファイル検証       │  拡張子・マジックナンバー確認
└──────┬───────────┘
       │ OK
       ▼
┌──────────────────┐
│ メタデータ取得     │  総ページ数・ページサイズ・暗号化有無
└──────┬───────────┘
       │
       ├──→ 暗号化あり → エラー表示「パスワード付き PDF には対応していません」
       │
       ▼
┌──────────────────┐
│ 1ページ目を表示    │  画像レンダリング + OCR テキスト抽出
└──────┬───────────┘
       │
       ├──→ [Worker] サムネイル生成を開始（バックグラウンド・順次）
       ├──→ [Worker] 2–3 ページ目をプリフェッチ
       │
       ▼
   UI 表示完了（操作可能状態）
```

### 6.2 PDF 保存フロー

```
ユーザー操作: Ctrl+S / 名前を付けて保存
       │
       ▼
┌──────────────────┐
│ 編集データ検証     │  空テキスト BB の警告
└──────┬───────────┘
       │
       ▼
┌──────────────────────────┐
│ [Worker] 一時ファイル書き出し │
│  ├─ 元 PDF を読み込み       │
│  ├─ 編集済みページの         │
│  │   テキストレイヤーを再構築  │
│  ├─ 未編集ページはそのまま    │
│  └─ 画像レイヤーは無変換     │
└──────┬───────────────────┘
       │
       ▼
┌──────────────────┐
│ 検証              │  一時ファイルが正常に読み込めるか
└──────┬───────────┘
       │ OK
       ▼
┌──────────────────┐
│ リネーム          │  一時ファイル → 正式ファイル名
└──────┬───────────┘
       │
       ▼
  isDirty フラグを全ページ false に
  ステータスバーの未保存マークを消去
```

---

## 7. 技術選定（確定）

| レイヤー | 選定技術 | 選定理由 |
|---------|---------|---------|
| フレームワーク | **Tauri v2** | Rust ベースの軽量・高速な実行環境。v2 移行により、プラグインシステムの柔軟性とセキュリティが向上。 |
| フロントエンド | **React 19** + **TypeScript** | 最新の React 19 を採用。Concurrent Rendering により大規模ドキュメントでも快適な操作感を実現。 |
| 状態管理 | **Zustand 5** | 超軽量な状態管理。Undo/Redo パックとの統合が容易で、ボイラープレートを最小化。 |
| PDF 解析・表示 | **pdfjs-dist 5.5** | 最新の pdf.js を採用。Vite 7 環境下での WebWorker 動作を Blob URL 戦略で最適化。 |
| PDF 編集・保存 | **pdf-lib 1.17** | 純 JS で軽量な PDF 操作。Content Stream の再構築による正確な読み順（Content Stream 順）制御を実現。 |
| ビルドツール | **Vite 7** | 次世代の高速ビルド。WebWorker の `?url` インポートによる Worker 分離戦略を採用。 |

### 技術的な重要事項（確定済み）

- **T-01: 高速レンダリング戦略**: WebWorker を Blob URL 化して確実に別スレッドで実行。パース処理による UI スレッドのブロックを皆無にする。
- **T-02: メモリキャッシュ**: `PDFPageProxy` をメモリ上にキャッシュ（`pageProxyCache`）し、ページ切り替えを 0ms に短縮。
- **T-03: Shared Proxy**: 同一ファイルへの複数リクエストを `Promise` ベースのキャッシュで一本化。無駄な PDF 解析コストを排除。
- **T-04: Asset Protocol**: Tauri v2 の `asset://` プロトコルを適切に構成（CSP 設定含む）。Range Request による大容量 PDF の部分読み込みを完全サポート。
- **T-05: Path Normalization**: Windows 環境特有のパス区切り（`\`）を正規化し、Tauri バックエンド/アセットプロトコルとの整合性を確保。

---

## 8. リスク・課題

| # | リスク | 影響度 | 発生可能性 | 対策案 |
|---|--------|--------|-----------|--------|
| R-01 | 数千ページ PDF でのメモリ逼迫 | 高 | 中 | 遅延読み込み + LRU キャッシュ（20 ページ）+ Worker スレッド。早期にプロファイリングで検証 |
| R-02 | 縦書き OCR の BB 座標解析が不正確 | 中 | 高 | pdf.js の縦書き対応状況を PoC で検証。不足なら独自パーサーを検討 |
| R-03 | PDF テキストレイヤーの形式が多様 | 高 | 高 | フォント埋め込み・CID フォント・Type3 フォント等を事前にサンプル検証。対応範囲を明確に定義 |
| R-04 | グループ化後の BB 座標計算で文字がズレる | 中 | 中 | 外接矩形の自動計算 + 手動微調整機能 |
| R-05 | 保存時にオリジナル PDF の画質劣化 | 高 | 中 | テキストレイヤーのみ差し替え。画像ストリームは無変換。保存前後のバイナリ検証をテストに含める |
| R-06 | pdf-lib で読み順制御ができない | 高 | 中 | Content Stream 内のテキストオブジェクト順序を制御する方法を PoC で検証。不可なら mupdf に切替 |
| R-07 | 大量ページの保存処理に時間がかかる | 中 | 高 | 未変更ページはストリームコピーで高速化。Worker で非同期処理。プログレスバーで体感時間を軽減 |

---

## 9. 用語定義

| 用語 | 定義 |
|------|------|
| OCR テキスト | PDF の透明テキストレイヤーに格納された文字情報。PDF ビューアでのコピペ・検索に使用される |
| テキストレイヤー | PDF の画像の上に重ねられた透明なテキスト層。視覚的には見えないが、選択・コピー・検索が可能 |
| バウンディングボックス（BB） | OCR テキストの位置・範囲を示す矩形領域。1 文字〜複数行にわたる場合がある |
| グループ化 | 複数のバウンディングボックスを 1 つに統合する操作。テキストは結合され、座標は外接矩形になる |
| 読み順 | PDF の Content Stream 内でテキストオブジェクトが出現する順序。コピペ時のテキスト出力順を決定する |
| 遅延読み込み（Lazy Loading） | 必要なページだけをオンデマンドでメモリに展開する手法 |
| LRU キャッシュ | Least Recently Used。最も長く使われていないデータから順に破棄するキャッシュ戦略 |
| 仮想スクロール | DOM に全要素を描画せず、可視範囲の要素のみを動的に描画する手法 |
| 外接矩形 | 複数の矩形を全て包含する最小の矩形 |
| Content Stream | PDF 内部でページの描画命令を記述するストリーム。テキストの出現順が読み順を決定する |

---

## 10. 今後のステップ

| # | フェーズ | 内容 | 備考 |
|---|---------|------|------|
| 1 | **要件定義レビュー** | 本ドキュメントの確認・修正 | ← 現在地 |
| 2 | **技術検証（PoC）** | T-01〜T-05 の検証。技術選定の確定 | 特に PDF ライブラリの選定が最重要 |
| 3 | **基本設計** | 画面設計・データモデル設計・アーキテクチャ設計 | |
| 4 | **実装 Phase 1** | PDF 読み込み・表示・サムネイル・ページ遷移 | 骨格となる UI |
| 5 | **実装 Phase 2** | OCR テキスト抽出・表示・編集・Undo/Redo | コア機能 |
| 6 | **実装 Phase 3** | グループ化・並べ替え・BB 追加・削除 | 高度な編集機能 |
| 7 | **実装 Phase 4** | PDF 保存・自動バックアップ・読み順制御 | 永続化 |
| 8 | **実装 Phase 5** | OCR 可視化オーバーレイ・UI 改善 | Nice to Have |
| 9 | **テスト・品質検証** | 各種 PDF サンプルでの動作検証・パフォーマンステスト | |
| 10 | **リリース** | インストーラー作成・配布 | |
�配布 | |
�� | |
