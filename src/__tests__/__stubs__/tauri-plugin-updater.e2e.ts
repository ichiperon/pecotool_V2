// E2E stub for @tauri-apps/plugin-updater (Playwright / Vite dev server).
// The real module is only available inside a Tauri runtime.
// This stub satisfies dynamic import() in useAppUpdater.ts when running
// in a plain browser context (no Tauri IPC), so the dev server can start.
export const check = async () => null;
