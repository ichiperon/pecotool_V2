/**
 * ブロック区切りスペース（案A）の共有定数。
 *
 * `pdfSaverCore.ts` と `pdfCurveTextRender.ts` の両経路で使用する。
 * 値を変更する場合はここ1箇所を修正すること。
 *
 * ## チューニング指針
 * Acrobat のテキスト抽出は座標と文字幅のヒューリスティクスで隣接 BB を連結する。
 * この定数が大きすぎると同一行の語が余分な空白で分断される (#4)。
 * 小さすぎると近接 BB の連結を抑止できない。
 * 初期値 0.8 はフォントサイズの 0.8em 相当を追加送り幅とする保守値。
 * Acrobat 実機テストでのチューニング推奨。
 */
export const BLOCK_SEPARATOR_EXTRA_ADVANCE_EM = 0.8;
